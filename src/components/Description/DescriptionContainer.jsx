import React from "react"
import './DescriptionContainer.css'

export default function DescriptionContainer({ children }) {
    return (
        <div className="descriptionB-container">{children}</div>
    )
}